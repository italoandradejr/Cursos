// junta elementos em um array em uma frase, por meio de um separador

let frase = "Testando o método split"

let palavras = frase.split(" ")

let novaFrase = palavras.toLocaleString('@')

console.log(novaFrase)